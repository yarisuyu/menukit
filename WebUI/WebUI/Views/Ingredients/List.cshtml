@model IEnumerable<WebUI.Models.Entities.Ingredient>

@{
    ViewBag.Title = "List";
}

<h2>List</h2>

<p>Hello from List</p>

@foreach (var ingredient in Model)
{
    <div class="Item">
        <h3>@ingredient.Name</h3>
        @ingredient.Category
        </div>
}